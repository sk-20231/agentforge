# Environment and secrets — NEVER commit these
.env
.env.*
!.env.example
*.pem
*.key
*credentials*
*secret*

# Virtual environment
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Test and tooling
.pytest_cache/
.coverage
htmlcov/

# Agent runtime data — generated at runtime, not part of source
agent_logs.jsonl
corpus.json
memory.json
/memory/

# Learning docs and roadmaps — local reference only, not part of source
docs/

# Test fixtures — intentionally tracked so CI can run the eval suite
# tests/fixtures/EmployeeHandbook.txt  ← NOT ignored (CI needs it)
# tests/eval_dataset.json              ← NOT ignored (CI needs it)

# AI assistant context files — local reference, not part of source
CLAUDE.md

# IDE and tooling
.cursor/
.vscode/
*.code-workspace

# Claude Code memory — personal session context, not source
.claude/
